# Optional: point the Next.js UI at the standalone Python API.
# Leave unset to use the built-in Next.js API routes for local demo mode.
NEXT_PUBLIC_ANALYSIS_API_URL=http://localhost:8000

# Python API input dataset, relative to the project root or an absolute path.
CHAINSCOPE_DATASET=data/fixtures/worm_cluster_transactions.json
